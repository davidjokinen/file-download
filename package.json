{
  "name": "file-download",
  "version": "1.0.0",
  "description": "Download files easier.",
  "main": "index.js",
  "scripts": {
    "build": "webpack --mode production",
    "watch": "webpack --watch --mode development",
    "examples": "npm run build; http-server -a localhost -p 8123 -c-1 ./test",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "David Jokinen",
  "license": "ISC",
  "devDependencies": {
    "@babel/core": "^7.4.0",
    "@babel/preset-env": "^7.4.2",
    "babel-loader": "^8.0.5",
    "http-server": "^0.11.1",
    "webpack": "^4.29.6",
    "webpack-cli": "^3.3.0"
  }
}
